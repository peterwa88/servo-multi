# servo_origin

This directory is reserved for the **official Servo source checkout**.

Do not hand-copy files here.

Initialize with:

```bash
./scripts/bootstrap_servo_origin.sh
```

After bootstrap, this directory should be a Git checkout of:
`https://github.com/servo/servo.git`
